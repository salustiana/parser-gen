<stmt> ::= `id` `=` <expr>`;`
	| `loop` `(`<optexpr>`;` <optexpr>`;` <optexpr>`)` <stmt>
	| `{` <stmts> `}`

<stmtlist> ::= <stmtlist> <stmt>
	| ``

<expr> ::= <expr> `+` <term>
	| <term>

<term> ::= <term> `*` <fact>
	| <fact>

<fact> ::= `(` <expr> `)`
	| `id`
