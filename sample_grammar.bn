<stmt> ::= `id` `=` <expr>`;`
	| `loop` `(`<optexpr>`;` <optexpr>`;` <optexpr>`)` <stmt>
	| `{` <stmtlist> `}`

<stmtlist> ::= <stmtlist> <stmt>
	| ``

<expr> ::= <expr> `+` <term>
	| <term>

<term> ::= <term> `*` <fact>
	| <fact>

<fact> ::= `(` <expr> `)`
	| `id`
